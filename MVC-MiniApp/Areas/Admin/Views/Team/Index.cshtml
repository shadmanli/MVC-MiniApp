@model IEnumerable<TeamVM>
<div class="container center-container">
    <h2 class="mt-5">Team</h2>
    <a asp-action="Create" class="btn btn-secondary">Create</a>
    <table class="table table-striped table-bordered table-hover mt-4">
        <thead class="table-dark">
            <tr>
                <th>Image</th>
                <th>Name</th>
                <th>Position</th>

                <th>Operations</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><img src="~/img/@item.Image" style="width:150px;height:150px" /></td>
                    <td>@item.Name</td>
                    <td>@item.Position</td>
                    <td>
                        <a asp-action="Detail" asp-route-id="@item.Id" class="btn btn-dark">Detail</a>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary">Edit</a>
                        <button data-id="@item.Id" class="btn btn-danger delete-expert-btn">Delete</button>
                    </td>
                </tr>

            }
        </tbody>
    </table>
</div>



